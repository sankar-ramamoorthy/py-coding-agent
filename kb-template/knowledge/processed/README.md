# processed/

Synthesized, but still pending promotion. A document here has been worked up from raw
material — organized, reviewed for accuracy, cross-referenced — but has not yet had its
`status` explicitly flipped to `canonical`/`active` as part of a deliberate promotion.

See `../../docs/promotion.md` for what moves a document from here into `../topics/`.
